services:
  sovereign-agent:
    build: .
    ports: ["8000:8000"]
    volumes:
      - ./workspace:/app/workspace
      - ./.sca:/app/.sca
    environment:
      - SCA_HOST=0.0.0.0
      - SCA_WORKSPACE=/app/workspace
